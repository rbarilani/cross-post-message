{
  "name": "cross-post-message",
  "version": "0.0.0",
  "description": "postMessage api cross domain",
  "main": "lib/index.js",
  "scripts": {
    "build": "browserify lib/index.js -o cross-post-message.js -s CrossPostMessage -t [ babelify --presets [ es2015 ] ]",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Ruben Barilani",
  "license": "MIT",
  "devDependencies": {
    "babel-preset-es2015": "^6.3.13",
    "babelify": "^7.2.0",
    "browserify": "^13.0.0",
    "lodash": "^4.2.1"
  }
}
